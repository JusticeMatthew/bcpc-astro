---
const { secondary, primary } = Astro.props;
---

<button
  class:list={[
    'flex items-center justify-center rounded-sm px-6 py-4 ring-brand transition-all duration-75 hover:text-brand hover:ring-1 active:scale-95',
    {
      'hover:bg-mid bg-brand font-semibold text-dark': primary,
    },
    {
      'bg-mid font-normal text-light': secondary,
    },
  ]}
>
  <slot />
</button>
